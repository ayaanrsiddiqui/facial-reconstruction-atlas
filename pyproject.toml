[project]
name = "entdatabase"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "openpyxl>=3.1.0",
    "pillow>=10.0.0",
]

[tool.vercel.scripts]
build = "python build.py"
